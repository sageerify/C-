#include<iostream> // is header file which works input and output
using namespace std; //this means we can use name for objects and variables from standard library
int main(){ //This is called function.code will execute inside it
cout<<"hello world"; //used together with insertion operator (<<) to output/input text. In our example it will output"Hello World"

return 0; //end the main function
	
}
